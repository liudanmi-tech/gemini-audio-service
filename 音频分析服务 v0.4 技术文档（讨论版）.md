# 音频分析服务 v0.4 技术文档（讨论版）

**文档版本**: v0.4

**创建日期**: 2026-01-16

**最后更新**: 2026-01-16

**状态**: 🚧 架构设计完成，待实施验证

**核心变更**: ✅ 技能化架构、场景自动识别、动态策略生成

---

## 📋 目录

1. [系统架构总览](#1-系统架构总览)

2. [v0.4 核心变更](#2-v04-核心变更)

3. [技能化架构设计](#3-技能化架构设计)

4. [技术栈选型](#4-技术栈选型)

5. [功能描述](#5-功能描述)

6. [技术实现方式](#6-技术实现方式)

7. [大模型调用协议](#7-大模型调用协议)

8. [API 接口文档](#8-api-接口文档)

9. [数据模型](#9-数据模型)

10. [技能库设计](#10-技能库设计)

11. [实施步骤](#11-实施步骤)

12. [关键文件清单](#12-关键文件清单)

---

## 1. 系统架构总览

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐

│ iOS 客户端 (SwiftUI) │

│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │

│ │ 登录模块 │ │ 录音模块 │ │ 任务列表 │ │

│ │ ✅ 已实现 │ │ ✅ 已实现 │ │ ✅ 已实现 │ │

│ └──────────────┘ └──────────────┘ └──────────────┘ │

│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │

│ │ 任务详情 │ │ 策略分析 │ │ 图片显示 │ │

│ │ ✅ 已实现 │ │ ✅ 已实现 │ │ ✅ 已实现 │ │

│ └──────────────┘ └──────────────┘ └──────────────┘ │

└─────────────────────────────────────────────────────────────┘

│

│ HTTP (Alamofire)

│ Authorization: Bearer {JWT Token}

▼

┌─────────────────────────────────────────────────────────────┐

│ FastAPI 后端服务 (Python 3.12) │

│ ┌────────────────────────────────────────────────────┐ │

│ │ 感知层 (Perception Layer) │ │

│ │ - 场景识别 (Scene Classification) │ │

│ │ - 上下文理解 (Context Understanding) │ │

│ └────────────────────────────────────────────────────┘ │

│ │ │

│ ▼ │

│ ┌────────────────────────────────────────────────────┐ │

│ │ 调度层 (Orchestration Layer) │ │

│ │ - 技能路由 (Skill Router) │ │

│ │ - 技能组合 (Skill Composition) │ │

│ │ - 优先级管理 (Priority Management) │ │

│ └────────────────────────────────────────────────────┘ │

│ │ │

│ ▼ │

│ ┌────────────────────────────────────────────────────┐ │

│ │ 执行层 (Execution Layer) │ │

│ │ - 技能加载 (Skill Loading) │ │

│ │ - Prompt组装 (Prompt Assembly) │ │

│ │ - 策略生成 (Strategy Generation) │ │

│ │ - 结果融合 (Result Fusion) │ │

│ └────────────────────────────────────────────────────┘ │

│ ┌────────────────────────────────────────────────────┐ │

│ │ 技能库 (Skill Registry) │ │

│ │ - workplace_jungle (职场丛林法则) │ │

│ │ - family_relationship (亲密关系技能) │ │

│ │ - education_communication (教育沟通技能) │ │

│ │ - brainstorm (头脑风暴技能) │ │

│ └────────────────────────────────────────────────────┘ │

└─────────────────────────────────────────────────────────────┘

│

│ HTTP (反向代理)

▼

┌─────────────────────────────────────────────────────────────┐

│ Google Gemini API │

│ - Gemini 3 Flash (场景识别、策略生成) │

│ - Gemini Nano Banana (图片生成) │

└─────────────────────────────────────────────────────────────┘
```

### 1.2 三层架构设计

v0.4 采用三层架构实现技能化策略生成：

1. **感知层 (Perception Layer)**
- 场景识别：分析对话转录，识别场景类型（职场/家庭/教育/头脑风暴）

- 上下文理解：提取对话中的关键信息
2. **调度层 (Orchestration Layer)**
- 技能路由：根据场景识别结果匹配对应技能

- 技能组合：支持多场景识别时的多技能组合

- 优先级管理：按技能优先级和场景置信度排序
3. **执行层 (Execution Layer)**
- 技能加载：从文件系统加载 SKILL.md 并解析

- Prompt组装：动态组装技能特定的 Prompt

- 策略生成：调用 Gemini 生成策略分析

- 结果融合：合并多个技能的结果，去重和排序

---

## 2. v0.4 核心变更

### 2.1 从固定 Prompt 到技能化架构

**v0.3 架构**：

- 使用固定的 Call #2 Prompt

- 所有场景使用相同的分析逻辑

- 难以针对不同场景优化

**v0.4 架构**：

- 采用 Claude Skills 格式的技能库

- 每个技能有独立的 Prompt 模板

- 根据场景自动选择并执行对应技能

- 支持多技能组合执行

### 2.2 新增功能

1. **场景自动识别**
- 使用 Gemini 3 Flash 进行场景分类

- 支持多场景识别（返回置信度）

- 自动匹配对应技能
2. **技能库系统**
- Claude Skills 格式（SKILL.md + 目录结构）

- 技能元数据存储在数据库

- Prompt 模板从文件系统动态加载

- 支持技能热更新
3. **多技能组合**
- 当识别到多个场景时，并行执行多个技能

- 自动合并结果，去重和排序

- 保留执行记录用于分析
4. **技能管理 API**
- 获取技能列表

- 获取技能详情

- 重新加载技能（刷新缓存）
5. **场景识别 API**
- 独立的场景识别接口（不生成策略）

- 用于调试和预览

---

## 3. 技能化架构设计

### 3.1 技能库结构（Claude Skills 格式）

```
skills/

├── workplace_jungle/ # 职场丛林法则技能

│ ├── SKILL.md # 主文档（YAML frontmatter + Prompt模板）

│ ├── scripts/ # 可执行代码（可选）

│ │ ├── preprocess.py # 预处理脚本

│ │ └── postprocess.py # 后处理脚本

│ ├── references/ # 参考文档（可选）

│ │ ├── knowledge_base.md # 知识库内容

│ │ └── examples.md # 示例

│ └── assets/ # 资源文件（可选）

│ └── templates/ # 模板文件

├── family_relationship/ # 亲密关系技能

│ ├── SKILL.md

│ └── ...

├── education_communication/ # 孩子教育沟通技能

│ ├── SKILL.md

│ └── ...

└── brainstorm/ # 头脑风暴技能

├── SKILL.md

└── ...
```

### 3.2 SKILL.md 格式

每个技能包含一个 `SKILL.md` 文件，格式如下：

```markdown
---

name: workplace_jungle

description: 适用于职场沟通、上下级关系、同事协作等场景的策略分析技能

category: workplace

priority: 100

version: "1.0.0"

enabled: true

keywords:

- "老板"

- "同事"

- "项目"

scenarios:

- "上下级沟通"

- "同事协作"

dependencies:

- python>=3.12

author: AI军师团队

---



# 职场丛林法则



## 技能概述

...



## Prompt模板



```prompt

角色: 你是一位精通博弈论、职场心理学与视觉修辞的深度沟通专家。

...

对话转录:

{transcript_json}
```

```
### 3.3 技能执行流程



1. **场景识别** → Router Agent 分析对话，识别场景类型

2. **技能匹配** → 根据场景类别匹配技能（支持多技能）

3. **技能加载** → 从文件系统加载 SKILL.md，解析 frontmatter 和 Prompt

4. **预处理**（可选）→ 执行 `scripts/preprocess.py`

5. **Prompt组装** → 替换变量（{transcript_json}等）

6. **知识库注入**（可选）→ 从 `references/knowledge_base.md` 读取并注入

7. **Gemini调用** → 使用组装的 Prompt 生成策略

8. **后处理**（可选）→ 执行 `scripts/postprocess.py`

9. **结果融合** → 如果多技能，合并结果并去重

10. **保存数据库** → 保存策略分析和执行记录



---



## 4. 技术栈选型



### 4.1 后端技术栈



- **Python 3.12**: 主要开发语言

- **FastAPI**: Web 框架，提供异步 API

- **SQLAlchemy (Async)**: 异步 ORM，操作 PostgreSQL

- **asyncpg**: PostgreSQL 异步驱动

- **Google Gemini API**:

- `gemini-3-flash-preview`: 场景识别和策略生成

- `gemini-2.5-flash-image`: 图片生成（Nano Banana）

- **Pydantic**: 数据验证和序列化

- **python-jose**: JWT Token 处理

- **oss2**: 阿里云 OSS SDK

- **PyYAML**: 解析 SKILL.md 的 YAML frontmatter



### 4.2 数据库



- **PostgreSQL 14+**: 关系型数据库（阿里云 RDS）

- **JSONB**: 存储灵活的 JSON 数据（技能元数据、策略分析结果等）



### 4.3 存储服务



- **阿里云 OSS**: 图片存储（私有 Bucket）

- **文件系统**: 技能库文件（SKILL.md、scripts、references）



### 4.4 前端技术栈（iOS）



- **SwiftUI**: UI 框架

- **Alamofire**: 网络请求库

- **Keychain**: 安全存储 JWT Token



---



## 5. 功能描述



### 5.1 核心功能



1. **音频上传与分析**

- 支持 m4a/mp3/wav 格式

- 自动转文字（Gemini 3 Flash）

- 说话人分离

- 情绪分析



2. **场景自动识别**（v0.4 新增）

- 分析对话内容，识别场景类型

- 支持多场景识别（返回置信度）

- 场景类型：职场、家庭、教育、头脑风暴、其他



3. **技能化策略生成**（v0.4 新增）

- 根据场景自动选择技能

- 使用技能特定的 Prompt 生成策略

- 支持多技能组合执行

- 自动去重和排序



4. **图片生成**

- 为关键时刻生成火柴人图片

- 使用 Gemini Nano Banana 模型

- 存储到阿里云 OSS



5. **用户认证**

- 手机号 + 验证码登录

- JWT Token 认证

- 用户数据隔离



### 5.2 技能库功能（v0.4 新增）



1. **技能管理**

- 技能列表查询

- 技能详情查看

- 技能重新加载（刷新缓存）



2. **技能执行**

- 自动场景识别和技能匹配

- 动态 Prompt 组装

- 支持预处理和后处理脚本

- 知识库注入



3. **多技能组合**

- 并行执行多个技能

- 结果自动合并

- 基于相似度去重

- 按优先级排序



---



## 6. 技术实现方式



### 6.1 场景识别实现



**文件**: `skills/router.py`



```python

async def classify_scene(transcript: str, gemini_api_key: str, proxy_url: Optional[str] = None) -> Dict:

"""

场景分类，调用Gemini Router Agent

返回格式:

{

"scenes": [

{

"category": "workplace",

"confidence": 0.95,

"reasoning": "对话涉及项目汇报和截止日期讨论"

}

],

"primary_scene": "workplace"

}

"""
```

**Prompt 设计**:

- 定义 5 种场景类型（workplace/family/education/brainstorm/other）

- 要求返回 JSON 格式，包含置信度和推理过程

- 支持多场景识别（返回所有匹配的场景）

### 6.2 技能匹配实现

**文件**: `skills/router.py`

```python
async def match_skills(

scene_result: Dict,

db: AsyncSession,

min_confidence: float = 0.5

) -> List[Dict]:

"""

根据场景分类结果匹配技能

匹配规则:

1. 只考虑置信度 >= min_confidence 的场景

2. 根据场景的 category 匹配技能的 category

3. 去重（基于 skill_id）

4. 排序：先按 priority（降序），再按 confidence（降序）

"""
```

### 6.3 技能加载实现

**文件**: `skills/loader.py`

1. **解析 SKILL.md**:
- 提取 YAML frontmatter（使用 PyYAML）

- 提取 "## Prompt模板" 部分的 Prompt 内容

- 支持代码块格式（```prompt ... ```）
2. **加载知识库**:
- 从 `references/knowledge_base.md` 读取（可选）

- 如果存在，注入到 Prompt 末尾
3. **执行脚本**:
- 支持 `scripts/preprocess.py` 和 `scripts/postprocess.py`

- 动态加载并执行 Python 模块

### 6.4 技能执行实现

**文件**: `skills/executor.py`

```python
async def execute_skill(

skill: Dict,

transcript: list,

context: Dict,

gemini_api_key: str,

skills_base_path: str = "skills"

) -> Dict:

"""

执行单个技能

流程:

1. 加载技能文件

2. 解析 SKILL.md

3. 预处理（可选）

4. 组装 Prompt（替换变量）

5. 注入知识库（可选）

6. 调用 Gemini

7. 后处理（可选）

8. 返回结果

"""
```

**Prompt 变量替换**:

- `{transcript_json}`: 对话转录（JSON 格式）

- `{session_id}`: 会话 ID

- `{user_id}`: 用户 ID

### 6.5 多技能组合实现

**文件**: `skills/composer.py`

```python
def compose_results(skill_results: List[Dict]) -> Dict:

"""

组合多个技能的结果

处理逻辑:

1. 合并所有技能的 strategies 数组

2. 去重：基于 title 和 content 相似度（> 0.8）

3. 排序：按 priority（降序）

4. 合并 visual 数据，按 transcript_index 排序

5. 限制 visual 数量（最多 5 个）

"""
```

**去重算法**:

- 使用 `difflib.SequenceMatcher` 计算文本相似度

- 如果 title 相似度 > 0.8 或 (title 相似度 > 0.6 且 content 相似度 > 0.7)，认为是重复

- 保留优先级更高的策略

---

## 7. 大模型调用协议

### 7.1 场景识别（Router Agent）

**模型**: `gemini-3-flash-preview`

**输入**:

```json
{

"transcript": [

{

"speaker": "Speaker_0",

"content": "这个项目什么时候能完成？",

"timestamp": "00:05"

}

]

}
```

**输出**:

```json
{

"scenes": [

{

"category": "workplace",

"confidence": 0.95,

"reasoning": "对话涉及项目汇报和截止日期讨论"

}

],

"primary_scene": "workplace"

}
```

### 7.2 策略生成（技能执行）

**模型**: `gemini-3-flash-preview`

**输入**: 技能特定的 Prompt（从 SKILL.md 加载）

**输出**:

```json
{

"strategies": [

{

"id": "s1",

"label": "借力打力",

"emoji": "⚔️",

"title": "策略标题",

"content": "### 建议话术\n1. **心理逻辑**: ...\n2. **推荐话术**: '...'"

}

],

"visual": [

{

"transcript_index": 3,

"speaker": "Speaker_1",

"image_prompt": "米色背景，极简火柴人线稿...",

"emotion": "紧张、防御",

"subtext": "感到被冒犯但试图保持礼貌",

"context": "对方提出不合理要求...",

"my_inner": "感到被冒犯但保持礼貌",

"other_inner": "试探对方的弹性"

}

]

}
```

### 7.3 图片生成

**模型**: `gemini-2.5-flash-image`

**输入**: `image_prompt`（从策略生成的 visual 数据中提取）

**输出**: PNG 图片（Base64 或上传到 OSS 返回 URL）

---

## 8. API 接口文档

### 8.1 技能管理接口（v0.4 新增）

#### GET /api/v1/skills - 获取所有可用技能

**认证**: 需要 JWT Token

**查询参数**:

- `category` (可选): 技能分类（workplace/family/education/brainstorm）

- `enabled` (可选, 默认 true): 是否只返回启用的技能

**响应**:

```json
{

"code": 200,

"message": "success",

"data": {

"skills": [

{

"skill_id": "workplace_jungle",

"name": "职场丛林法则",

"description": "适用于职场沟通、上下级关系、同事协作等场景",

"category": "workplace",

"priority": 100,

"enabled": true,

"version": "1.0.0"

}

]

}

}
```

#### GET /api/v1/skills/{skill_id} - 获取特定技能详情

**认证**: 需要 JWT Token

**响应**: 包含技能的完整信息（不包括 Prompt 模板，Prompt 从文件系统读取）

#### POST /api/v1/skills/{skill_id}/reload - 重新加载技能

**认证**: 需要 JWT Token

**功能**: 从文件系统重新解析 SKILL.md，更新数据库缓存

### 8.2 场景识别接口（v0.4 新增）

#### POST /api/v1/tasks/sessions/{session_id}/classify-scene - 仅进行场景识别

**认证**: 需要 JWT Token

**功能**: 仅进行场景识别，不生成策略（用于调试和预览）

**响应**:

```json
{

"code": 200,

"message": "success",

"data": {

"scenes": [

{

"category": "workplace",

"confidence": 0.95,

"reasoning": "对话涉及项目汇报和截止日期讨论"

}

],

"primary_scene": "workplace",

"matched_skills": [

{

"skill_id": "workplace_jungle",

"name": "职场丛林法则",

"priority": 100

}

]

}

}
```

### 8.3 策略生成接口（更新）

#### POST /api/v1/tasks/sessions/{session_id}/strategies - 生成策略分析

**认证**: 需要 JWT Token

**功能**:

- 自动场景识别（调用 Router Agent）

- 自动技能匹配（根据场景类别匹配技能）

- 动态生成策略（使用匹配技能的 Prompt 模板）

**响应**:

```json
{

"code": 200,

"message": "success",

"data": {

"strategies": [...],

"visual": [...],

"applied_skills": [

{"skill_id": "workplace_jungle", "priority": 100}

],

"scene_category": "workplace",

"scene_confidence": 0.95

}

}
```

**注意**: 如果数据库中已存在策略分析，直接返回；否则使用技能化架构生成新的策略。

---

## 9. 数据模型

### 9.1 数据库表结构

#### skills 表（v0.4 新增）

```sql
CREATE TABLE skills (

skill_id VARCHAR(100) PRIMARY KEY,

name VARCHAR(200) NOT NULL,

description TEXT,

category VARCHAR(50) NOT NULL,

skill_path VARCHAR(500) NOT NULL,

priority INTEGER DEFAULT 0,

enabled BOOLEAN DEFAULT true,

version VARCHAR(50),

metadata JSONB DEFAULT '{}',

created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),

updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()

);
```

#### skill_executions 表（v0.4 新增）

```sql
CREATE TABLE skill_executions (

id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),

session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,

skill_id VARCHAR(100) NOT NULL REFERENCES skills(skill_id),

scene_category VARCHAR(50),

confidence_score JSON,

execution_time_ms INTEGER,

success BOOLEAN DEFAULT true,

error_message TEXT,

created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()

);
```

#### strategy_analysis 表（更新）

**新增字段**:

```sql
ALTER TABLE strategy_analysis

ADD COLUMN applied_skills JSONB DEFAULT '[]',

ADD COLUMN scene_category VARCHAR(50),

ADD COLUMN scene_confidence JSON;
```

---

## 10. 技能库设计

### 10.1 已实现的技能

1. **workplace_jungle**（职场丛林法则）
- category: `workplace`

- priority: 100

- 适用场景: 上下级沟通、同事协作、项目讨论
2. **family_relationship**（亲密关系技能）
- category: `family`

- priority: 90

- 适用场景: 夫妻沟通、亲子关系、家庭决策
3. **education_communication**（孩子教育沟通技能）
- category: `education`

- priority: 85

- 适用场景: 学习辅导、成长引导、教育沟通
4. **brainstorm**（头脑风暴技能）
- category: `brainstorm`

- priority: 80

- 适用场景: 创意讨论、方案设计、问题解决

### 10.2 添加新技能

1. 在 `skills/` 目录下创建新技能目录

2. 创建 `SKILL.md` 文件

3. 调用 `POST /api/v1/skills/{skill_id}/reload` 注册技能

---

## 11. 实施步骤

### 11.1 数据库迁移

1. 执行 `database/migrations/add_skills_tables.sql` 创建新表

2. 更新 `strategy_analysis` 表，添加新字段

### 11.2 技能库初始化

1. 创建技能目录结构

2. 为每个技能编写 `SKILL.md` 文件

3. 调用 API 注册技能到数据库

### 11.3 代码集成

1. 技能模块已实现（`skills/` 目录）

2. API 端点已创建（`api/skills.py`）

3. `main.py` 已更新，集成技能化架构

### 11.4 测试验证

1. 测试场景识别功能

2. 测试技能匹配和执行

3. 测试多技能组合

4. 验证策略生成结果

---

## 12. 关键文件清单

### 12.1 后端文件

- `skills/__init__.py` - 技能模块初始化

- `skills/loader.py` - 技能加载器

- `skills/registry.py` - 技能注册表

- `skills/router.py` - 场景路由器

- `skills/executor.py` - 技能执行器

- `skills/composer.py` - 多技能组合器

- `api/skills.py` - 技能管理 API

- `database/models.py` - 数据库模型（已更新）

- `main.py` - 主应用（已集成新架构）

### 12.2 技能目录

- `skills/workplace_jungle/SKILL.md`

- `skills/family_relationship/SKILL.md`

- `skills/education_communication/SKILL.md`

- `skills/brainstorm/SKILL.md`

### 12.3 数据库迁移

- `database/migrations/add_skills_tables.sql`

---

## 总结

v0.4 版本实现了从固定 Prompt 到技能化架构的升级，核心特性包括：

1. ✅ **场景自动识别** - 使用 Gemini 3 Flash 自动识别对话场景

2. ✅ **技能化架构** - Claude Skills 格式的技能库系统

3. ✅ **动态策略生成** - 根据场景自动选择并执行对应技能

4. ✅ **多技能组合** - 支持多场景识别时的多技能并行执行

5. ✅ **技能管理 API** - 完整的技能管理接口

该架构为未来扩展新技能提供了良好的基础，可以轻松添加新的场景类型和对应的分析技能。

---

**文档版本**: v0.4

**创建日期**: 2026-01-16

**状态**: 🚧 架构设计完成，待实施验证
